<UserControl x:Class="DoAn.Views.LichSuKhamControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" Margin="10">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Lịch sử các lần khám bệnh" Grid.Column="0" Margin="0,0,10,0" Padding="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Chọn một phiếu khám để xem chi tiết:" FontStyle="Italic" Foreground="#666" Margin="0,0,0,10"/>

                <DataGrid x:Name="dgLichSu" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          SelectionMode="Single" SelectionChanged="DgLichSu_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ngày Khám" Binding="{Binding NgayKham, StringFormat=dd/MM/yyyy HH:mm}" Width="130"/>
                        <DataGridTextColumn Header="Bác Sĩ Khám" Binding="{Binding TenBacSi}" Width="150"/>
                        <DataGridTextColumn Header="Chẩn Đoán" Binding="{Binding ChanDoan}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <GroupBox Header="Chi tiết Kết quả &amp; Đơn thuốc" Grid.Column="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#F8F9FA" Padding="15" CornerRadius="8" Margin="0,0,0,15" BorderBrush="#E9ECEF" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Mã Phiếu:" FontWeight="SemiBold" Grid.Row="0" Grid.Column="0" Margin="0,0,0,10"/>
                        <TextBlock x:Name="txtMaPhieu" Text="---" Grid.Row="0" Grid.Column="1" Foreground="#2196F3" FontWeight="Bold"/>

                        <TextBlock Text="Triệu chứng:" FontWeight="SemiBold" Grid.Row="1" Grid.Column="0" Margin="0,0,0,10"/>
                        <TextBlock x:Name="txtTrieuChung" Text="---" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" Margin="0,0,0,10"/>

                        <TextBlock Text="Kết luận (Bệnh):" FontWeight="SemiBold" Grid.Row="2" Grid.Column="0"/>
                        <TextBlock x:Name="txtChanDoan" Text="---" Grid.Row="2" Grid.Column="1" TextWrapping="Wrap" Foreground="#D35400" FontWeight="Bold"/>
                    </Grid>
                </Border>

                <TextBlock Text="💊 ĐƠN THUỐC BÁC SĨ KÊ:" Grid.Row="1" FontWeight="Bold" FontSize="14" Foreground="#2C3E50" Margin="0,0,0,10"/>

                <DataGrid x:Name="dgDonThuoc" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" Background="White">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên Thuốc" Binding="{Binding TenThuoc}" Width="200"/>
                        <DataGridTextColumn Header="ĐVT" Binding="{Binding DonViTinh}" Width="60"/>
                        <DataGridTextColumn Header="Số Lượng" Binding="{Binding SoLuong}" Width="80"/>
                        <DataGridTextColumn Header="Cách Dùng" Binding="{Binding CachDung}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>